This repo covers artifacts that address the following subjects

Database agnostic CRUD actions via Java API.

Modernization techniques from HTML FRAMESET‐FRAME into a Angular SPA.

Online dynamic chart generation using realtime DB data in UI Layer.

Session Metadata Management (XACML Approach).

Note: For more hypothetical enlargement, navigate to the wiki section of the repository.
